# 금새인터랙티브 블로그 지수관리 제안서

최종 수정본 개발자 전달용 소스입니다.

## 반영된 최신 내용

- 서로이웃 관련 내용 전체 삭제
- 체류시간 방문자 상품만 유지
- 엘리트형: 일평균 방문자 300명 활성화 / 월 13만원
- 노블형: 일평균 방문자 1,000명 활성화 / 월 22만원
- 로얄형: 일평균 방문자 2,000명 활성화 / 월 40만원
- 운영 축 3개가 전체 폭을 균등하게 사용하도록 조정
- 가격 안내 문구를 `방문 수는 선택한 상품 유형에 따라 달라집니다.`로 변경
- 공감·댓글 상품 영역 전체 삭제

## 실행 환경

- Node.js 22.13 이상
- npm

## 로컬 실행

```bash
npm ci
npm run dev
```

개발 서버가 안내하는 로컬 주소로 접속합니다.

## 빌드

```bash
npm run build
```

빌드 결과물은 `dist/` 폴더에 생성됩니다.

## 주요 수정 파일

- `app/page.tsx`: 제안서의 문구·상품·섹션 구성
- `app/globals.css`: 전체 디자인과 반응형 레이아웃
- `public/`: 로고, 파비콘, 공유 이미지 등 정적 이미지
- `app/layout.tsx`: 페이지 제목과 메타데이터

## 현재 운영 주소

https://gumse-blog-growth.viyorr.chatgpt.site/

## 참고

- `node_modules/`, `dist/`, 로컬 캐시 파일은 압축파일에서 제외했습니다.
- 설치 후 `npm run build`로 최종 빌드 상태를 확인할 수 있습니다.
